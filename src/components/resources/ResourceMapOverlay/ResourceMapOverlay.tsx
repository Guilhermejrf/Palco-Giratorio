import { MapVivo } from '../../map/MapVivo/MapVivo'

export const ResourceMapOverlay = () => (
  <div className="h-[420px] overflow-hidden rounded-2xl border border-[var(--color-cream-dark)]">
    <MapVivo />
  </div>
)
